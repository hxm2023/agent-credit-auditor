"""Estimator plugins (design §9). Distributions are exact: every cycle
outcome is a WeightedVector whose weights sum to 1 (a full probability
distribution over estimator values). Plugins never import oracles."""
