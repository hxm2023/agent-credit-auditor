# Marathon Prompt (mission order — CLAUDE.md is the constitution)

```
/research-pipeline "Execute the Agent-RL Credit Auditor engineering project. READ
CLAUDE.md in the project root FIRST (constitution: CPU-first audit tool, not a new
algorithm; four audit questions; clean new repo; old failures as regression fixtures;
claims policy). AUTHORITATIVE DESIGN: Agent-RL-Credit-Auditor_详细项目设计与旧项目迁移手册.md
(project root) — read fully before implementation; it locks formal objects, schema,
exact worlds, estimator plugin contract, independent oracle, audit gates, regression
package, protocol-first runner, 7-day plan, budget, claims policy. ARIS role is
execution discipline, NOT re-discovery: run the 7-day plan with per-day review;
before each audit gate run an independent cross-model review (oracle independence,
budget matching, split freezing, mechanism checks, claim honesty — reviewer fallback
per shared-references/reviewer-fallback.md). Decision log for deviations BEFORE first
formal run. Honesty: no post-hoc gate loosening, frozen fixtures versioned not
overwritten, published numbers traceable to artifact+commit+checksum; resume claims
only per design doc §23 (allowed claims). Server TBD (CPU-first — local laptop
sufficient; GPU candidates jindun/autodl1 reserved). Outputs: gate + result/manifest/
report per run (no-overwrite), release package with regression results + LICENSE +
claims-limited resume bullets. Human owns the project. Start with protocol freeze +
reproducing the 56 legacy CPU tests as regression seeds." --deep_mode: false,
auto_write: true, auto_proceed: true, venue: "engineering release (not a paper)"
```
